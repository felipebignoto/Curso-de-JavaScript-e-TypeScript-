// string, number, undefined, bool
//para checar o tipo -> console.log(typeof nomeVariavel);

const nome ="Felipe";//string
const nome1 = 'bignoto';//string

const num1 =10;//number
const num2 = 2.10;//number

let nomeAluno;//undefined
let sobrenomeluno = null; //undefined

const aprovado = true;//booleano( 1 ou 0)

